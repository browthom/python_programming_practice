# Press the green button in the gutter to run the script.
from interview_bit import find_majority_element

if __name__ == '__main__':
    a = [1, 1, 4]
    b = [2, 2, 2]
    c = [1, 2, 2, 2, 3, 3]
    #
    # # print(get_starting_gas_station(a, b))
    print(find_majority_element(c))
